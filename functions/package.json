{
  "name": "appquiz-functions",
  "description": "Firebase Cloud Functions for eLearn (Laban dictionary proxy)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0"
  },
  "private": true
}
